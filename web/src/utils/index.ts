import formatDate from './formatDate';
import store from './store';
import { uploadUrl, setAuthorization } from './uploadUrl';

export default {
  store,
  formatDate,
  uploadUrl,
  setAuthorization
};